<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for?(:title) ? ("#{yield(:title)} - Josh Pigford").html_safe : "Josh Pigford" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">

    <meta name="title" content="<%= content_for?(:title) ? ("#{yield(:title)} - Josh Pigford").html_safe : "Josh Pigford" %>">
    <meta name="description" content="<%= content_for?(:description) ? (yield(:description)) : "Josh Pigford: Maker. Dabbler. Founder and CEO of Maybe." %>">

    <!-- Open Graph / Facebook -->
    <meta property="og:type" content="website">
    <meta property="og:title" content="<%= content_for?(:title) ? ("#{yield(:title)} - Josh Pigford").html_safe : "Josh Pigford" %>">
    <meta property="og:description" content="<%= content_for?(:description) ? (yield(:description)) : "Josh Pigford: Maker. Dabbler. Founder and CEO of Maybe." %>">
    <meta property="og:image" content="<%= image_url(content_for?(:meta_image) ? yield(:meta_image) : "meta.jpg") %>">

    <!-- Twitter -->
    <meta property="twitter:card" content="summary_large_image">
    <meta property="twitter:title" content="<%= content_for?(:title) ? ("#{yield(:title)} - Josh Pigford").html_safe : "Josh Pigford" %>">
    <meta property="twitter:description" content="<%= content_for?(:description) ? (yield(:description)) : "Josh Pigford: Maker. Dabbler. Founder and CEO of Maybe." %>">
    <meta property="twitter:image" content="<%= image_url(content_for?(:meta_image) ? yield(:meta_image) : "meta.jpg") %>">

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Space+Mono:ital,wght@0,400;0,700;1,400;1,700&display=swap" rel="stylesheet">
    <script src="https://kit.fontawesome.com/b829d8d3c1.js" crossorigin="anonymous"></script>
    
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>

  <body class="lowercase">
    <header class="flex mb-10">
      <div class="flex-none mr-8">
        <%= link_to image_tag("headshot-collage.jpg", alt: 'Josh Pigford', class: 'w-36 h-36 rounded-full'), root_path %>
      </div>
      <div class="flex-auto mt-3">
        <h1 class="mb-3 text-3xl font-black"><%= link_to "Josh Pigford", root_path, class: 'text-black', rel: 'me' %> <span><a href="https://twitter.com/Shpigford" rel="me" class="text-black hover:bg-transparent hover:text-blue-700"><i class="text-2xl fab fa-twitter"></i></a></span></h1>
        <p class="mb-4">Maker. Dabbler. I can't stop starting things. Bearded.</p>
        <p class="font-bold">&rarr;
          <%= link_to 'Art', arts_path %>, <%= link_to 'Articles', articles_path %>, <%= link_to 'Investments', investments_path %>, <%= link_to 'Interviews', interviews_path %>, <%= link_to 'Music', music_path %>, <%= link_to 'Now', now_path %>, <%= link_to 'Projects', projects_path %>, <%= link_to 'Podcasts', podcasts_path %>, <%= link_to 'Reading', reading_path %>, <%= link_to 'Gear', gear_path %>, <%= link_to 'Toys', toys_path %>, <%= link_to 'NFTs', nfts_path %>
        </p>
      </div>
    </header>
    
    <main>
      <%= yield %>
    </main>

    <footer class="mt-20 text-sm text-gray-400">
      Site last updated on <b><%= last_updated.to_datetime.in_time_zone('Central Time (US & Canada)').strftime('%B %e, %Y at %l:%M%P %Z') %></b>. <a href="https://github.com/Shpigford/joshpigford.com/commits/main">Changelog</a>.
    </footer>

    <script src="https://cdn.usefathom.com/3.js" site="PKXZNVYB"></script>
  </body>
</html>
