<%- 
  title "Articles"
  description "A collection of fine writing by Josh Pigford" 
-%>

<div class="prose max-w-none">
  <h1>Articles</h1>

  <ul>
  <% @articles.each do |article| %>
     <li>
      <%= link_to article.title, article_path(article.slug) %> <time><%= article.publish_at.strftime('%B %e, %Y') %></time></li>
  <% end %>
  </ul>
</div>